import { IParserData } from '@antv/l7-core';
interface IImageCfg {
    extent: [number, number, number, number];
}
export default function image(data: string | string[], cfg: IImageCfg): IParserData;
export {};
