import { IParserData } from '@antv/l7-core';
interface IJoinOption {
    sourceField: string;
    targetField: string;
    data: any[];
}
/**
 *
 * @param data
 * @param options
 */
export declare function join(geoData: IParserData, options: IJoinOption): IParserData;
export {};
