export default class Clock {
    private autoStart;
    private startTime;
    private oldTime;
    private running;
    private elapsedTime;
    constructor(autoStart?: boolean);
    start(): void;
    stop(): void;
    getElapsedTime(): number;
    getDelta(): number;
}
