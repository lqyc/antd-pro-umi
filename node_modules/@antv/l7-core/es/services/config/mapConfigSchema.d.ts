declare const _default: {
    properties: {
        zoom: {
            type: string;
            minimum: number;
            maximum: number;
        };
        minZoom: {
            type: string;
            minimum: number;
            maximum: number;
        };
        maxZoom: {
            type: string;
            minimum: number;
            maximum: number;
        };
        center: {
            type: string;
            items: {
                type: string;
            };
            maxItems: number;
            minItems: number;
        };
        pitch: {
            type: string;
        };
    };
};
/**
 * 地图 Schema
 */
export default _default;
