import { ILayer } from '../../layer/ILayerService';
import { PassType } from '../IMultiPassRenderer';
import BaseNormalPass from './BaseNormalPass';
/**
 * RenderPass，负责输出到后续 PostProcessor 的 readFBO 中
 */
export default class RenderPass<InitializationOptions = {}> extends BaseNormalPass<InitializationOptions> {
    getType(): PassType;
    getName(): string;
    init(layer: ILayer, config?: Partial<InitializationOptions>): void;
    render(layer: ILayer): void;
}
