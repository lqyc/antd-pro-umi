import { BasicTarget } from '../utils/dom';
interface Position {
    left: number;
    top: number;
}
export declare type Target = BasicTarget<HTMLElement | Document>;
export declare type ScrollListenController = (val: Position) => boolean;
declare function useScroll(target?: Target, shouldUpdate?: ScrollListenController): Position;
export default useScroll;
