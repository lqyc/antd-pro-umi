"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _default = api => {
  // disable by default
  if (!api.userConfig.crossorigin) return;
  api.describe({
    key: 'crossorigin',
    config: {
      schema(joi) {
        return joi.alternatives(joi.boolean(), joi.object({
          include: joi.array().items(joi.object().instance(RegExp))
        }));
      }

    }
  });
  api.chainWebpack(webpackConfig => {
    webpackConfig.output.crossOriginLoading('anonymous');
    return webpackConfig;
  });
  const opts = api.userConfig.crossorigin || {};
  const include = opts.include || []; // last exec

  api.modifyHTML({
    fn: $ => {
      $('script').each((i, elem) => {
        const el = $(elem);
        const scriptSrc = el.attr('src');

        if (!scriptSrc) {
          return;
        } // 在 local 的 script 标签上添加 crossorigin="anonymous"


        if (!/^(https?:)?\/\//.test(scriptSrc)) {
          el.attr('crossorigin', 'anonymous');
        }

        if (include.some(reg => reg.test(scriptSrc))) {
          el.attr('crossorigin', 'anonymous');
        }
      });
      return $;
    },
    stage: Infinity
  });
};

exports.default = _default;