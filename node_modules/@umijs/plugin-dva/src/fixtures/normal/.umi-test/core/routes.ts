// @ts-nocheck
import { ApplyPluginsType } from '/Users/chencheng/Code/github.com/umijs/plugins/node_modules/@umijs/runtime';
import { plugin } from './plugin';

export function getRoutes() {
  const routes = [
  {
    "path": "/",
    "component": require('/Users/chencheng/Code/github.com/umijs/plugins/packages/plugin-dva/src/fixtures/normal/pages/index').default,
    "exact": true
  }
];

  // allow user to extend routes
  plugin.applyPlugins({
    key: 'patchRoutes',
    type: ApplyPluginsType.event,
    args: { routes },
  });

  return routes;
}
